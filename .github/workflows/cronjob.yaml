name: CronJob
on:
  pull_request:

jobs:
  list_unreleased:
    runs-on: ubuntu-latest
    outputs:
      vs_tags: ${{ steps.generate.outputs.vs_tags }}
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: "3.7"
      - uses: abatilo/actions-poetry@v2
        with:
          poetry-version: "1.1.15"
      - name: Generate Unreleased VSCode Tags
        id: generate
        run: |
          poetry install
          TAGS=$(poetry run python hack/tools.py list-unreleased-tags)
          echo ::set-output name=vs_tags::${TAGS}

  release_tag:
    runs-on: ubuntu-latest
    needs:
      - list_unreleased
    strategy:
      fail-fast: false
      matrix:
        vs_tag: ${{ fromJSON(needs.list_unreleased.outputs.vs_tags) }}
    steps:
      - name: release ${{ matrix.vs_tag }}
        run: echo ${{ matrix.vs_tag }}
